
# Live data — local per machine, never pushed (binary/secret/changing)
# Playwright/browser profiles — NEVER commit (passwords unrecoverable)
# Runtime
# Secrets
*-profile/
*.db
*.db-shm
*.db-wal
*.log
*.sqlite
.env
.playwright-mcp/
config/secrets.json
data/
data/sniff/
node_modules/
output/
profiles/
screenshots/
