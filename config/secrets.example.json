{
  "_note": "SHARED credentials only (not per-site). Per-site WordPress creds live in the sites table (add/edit via the dashboard Settings). Copy this file to config/secrets.json and fill. secrets.json is gitignored.",

  "dolphinAnty": {
    "apiToken": "REUSE from ../recipe-automator (same Dolphin account)",
    "localApi": "http://localhost:3001",
    "cloudApi": "https://dolphin-anty-api.com"
  },

  "pinclicks": {
    "email": "",
    "password": ""
  },

  "gemini": {
    "apiKeys": ["OPTIONAL — for AI-image vetting; reuse from recipe-automator flow accounts"]
  }
}
